"use client";
import { useEffect, useState } from "react";
import { account } from "@/lib/appwrite";

type User = { name: string; email: string; prefs: { role?: "customer" | "delivery" } };
export default function Dashboard() { const [user, setUser] = useState<User | null>(null); const [ready, setReady] = useState(false); useEffect(() => { account.get<User>().then(setUser).catch(() => window.location.assign("/auth")).finally(() => setReady(true)); }, []); if (!ready) return <main className="loading">Loading your FoodieFly account...</main>; if (!user) return null; const driver = user.prefs.role === "delivery"; return <main className="dashboard"><header className="header"><a className="brand" href="/"><i>F</i>oodieFly</a><button className="signOut" onClick={async () => { await account.deleteSession({ sessionId: "current" }); window.location.assign("/"); }}>Sign out</button></header><section><p className="eyebrow">{driver ? "DELIVERY DRIVER PORTAL" : "CUSTOMER ACCOUNT"}</p><h1>Hello, {user.name || user.email.split("@")[0]}.</h1><p className="intro">{driver ? "Your delivery dashboard is ready. New orders will appear here when the order system is connected." : "Ready for something delicious? Your orders and delivery updates will appear here."}</p><div className="dashboardCard"><span>{driver ? "DRIVER" : "CUSTOMER"}</span><h2>{driver ? "No delivery requests yet" : "No active orders yet"}</h2><p>{driver ? "Keep this page open to receive new delivery requests." : "Browse the menu to start your first FoodieFly order."}</p><a className="primary" href="/">{driver ? "View delivery status" : "Explore the menu"}</a></div></section></main>; }